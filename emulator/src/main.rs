fn main() -> Result<(), String> {
    // TODO: Refactor so this can also be run via CLI, with must of the old "go" function in play, here.
    print!("Note: CLI Access is not yet happening.");
    Ok(())
}
